# AST — план поднятия проекта (последовательность действий и переноса)

_Обновлено: 2026-07-06. **Статус: ПЛАН на утверждение. В этот репозиторий ещё ничего не перенесено** — переносим только после твоего «го» по этому плану, фаза за фазой._

Этот документ — не отчёт о сделанном, а **план**: в каком порядке доводим проект до ума и что, когда и куда переносим. Каждая фаза заканчивается стоп-точкой (отдельный PR → ты смотришь диффом → мерджишь), чтобы ты видела и подтверждала каждый шаг.

---

## 0. Где мы сейчас (факт)

- ✅ Репозиторий создан (этот, `Aros-Studio-Tokenomics`).
- ✅ Ревью исходника готово — `MIGRATION_REVIEW.md` (разбор всех 483 файлов).
- ✅ **Канон-версия слоёв уже написана** и лежит в старом репозитории `AST-Aros-Financial-Paradigm` (`main`, после PR #546). В этот чистый репозиторий она пока **НЕ перенесена** — это предмет плана ниже.
- ✅ **В старом репо канон-чистка добита** (PR #547–#549): слой 11 удалён (slashing перезаземлён в 10), `src/` очищен от AFC-идентификаторов, `ArosCoinReserveManager.sol` переписан в канон, **`01_coin_engine` снесён — инвариантный стержень I1–I8 переехал в корневой `CANON.md`** (TokenSpec → 03, node-спека → 02).
- ✅ **Техническая обвязка этого репозитория поднята**: Node 20, `package.json` (NestJS/TypeORM/pg — ровно то, что импортирует канонический `src/`), tsconfig/jest/eslint/prettier, CI сборки-линта-тестов (`ci.yml`) рядом с канон-гейтом, Docker (`Dockerfile` + `docker-compose.yml`: postgres + app), SessionStart-хук для Claude Code on the web. Сборка/линт/тест/гейт — зелёные.

---

## 1. Целевая структура чистого репозитория

```
Aros-Studio-Tokenomics/
├── CANON.md                  # стержень канона: инварианты I1–I8, константы, единый цикл (немодульный, над слоями)
├── README.md                 # обзор проекта
├── PROJECT_STATUS.md         # этот план/трекер
├── MIGRATION_REVIEW.md       # ревью исходника
├── LICENSE, .gitignore
├── 02_nodechain_engine/      # \
├── 03_token_management_layer/#  |
├── 06_governance_layer/      #  |
├── 07_processing_layer/      #  |  канон-слои (переносим в Фазе 2)
├── 08_fee_distribution/      #  |
├── 10_proof_of_transaction_engine/
├── 13_extra_supervisory_layer/
├── 14_decentralized_tx_encoding/
├── 15_reserve/               # /
├── src/, reference/, smart_contracts/   # код (Фаза 4, после канон-чистки)
└── docs/                     # спецификации/легал (Фаза 5, после канон-чистки)
```

_(`01_coin_engine` из структуры исключён по ратифицированному решению: эмиссия — следствие PoT + Token Management, не отдельный слой. Его стержень живёт в `CANON.md`.)_

---

## 2. Последовательность действий (фазы)

| Фаза | Что делаем | Стоп-точка |
|---|---|---|
| **Ф1 — Утвердить план** ← *сейчас* | Ты подтверждаешь эту структуру, порядок и реестр «переносим/не переносим». | твой «го» |
| **Ф2 — Перенос CANON.md + 9 слоёв** | Переносим `CANON.md` и канон-слои из старого репо (список в §3, 89 файлов), прогоняем firewall+лексика гейт. | PR → ты мерджишь |
| **Ф3 — Слой 11** | ✅ РЕШЕНО и исполнено в старом репо (PR #547): слой 11 удалён, slashing перезаземлён в `10/pot_slashing_conditions.md`. Сюда не переносится. | — |
| **Ф4 — Код в канон** | ✅ Канон-чистка кода сделана в старом репо (PR #547: `AFC_RESERVE`→`SYSTEM_RESERVE` и пр.; PR #548: `ArosCoinReserveManager.sol` переписан). Осталось: перенести `src/` + `reference/` + `smart_contracts/` сюда. | PR |
| **Ф5 — `docs/` в канон** | Канон-чистка спецификаций/легал/архитектуры (11 файлов с `AFC`). | PR |
| **Ф6 — Мусор + CI** | Не переносим `AGENT_*_REPORT`, `ci_logs/`, `reports/`, `failed_*`; правим `nightly-audit.yml` («инварианты AFC» → AST). | PR |
| **Ф7 — Финальный гейт** | firewall-греп по всему репозиторию → пусто; сборка/линт зелёные. | PR |

---

## 3. Реестр: что переносим / не переносим

**Переносим (готово в старом репо, канон-чисто) — Фаза 2:**
`CANON.md` (стержень I1–I8), `02_nodechain_engine`, `03_token_management_layer` (+ `AROS_Coin_TokenSpec.json`), `06_governance_layer` (новый, иерархия ИИ), `07_processing_layer`, `08_fee_distribution` (Variant A), `10_proof_of_transaction_engine`, `13_extra_supervisory_layer` (Eye с вето), `14_decentralized_tx_encoding`, `15_reserve` (новый). — **89 файлов.**

**Ждёт решения / доработки (Фазы 4–5):**
код `src/`+`reference/`+`smart_contracts/` (уже канон-чист в старом репо — перенос), `docs/` (11 файлов с `AFC`), `frontend/` (нужен ли UI здесь).

**Не переносим — исключено из канона:**
`01_coin_engine` (снесён: эмиссия = следствие PoT + Token Management; стержень → `CANON.md`), `04_aros_value_circulation`, `05_bridge_layer`, `09_crypto_ingestion_pipeline`, `11_node_security_and_payments` (залоги/стейкинг против I6; удалён в старом репо), `12_nodechain_ai_agents` (выкинуто совсем), форки `ast/extra_supervisory_layer` и `aros-tokenomics/03`, мусор (`AGENT_*_REPORT`, `ci_logs/`, `reports/`, `failed_*`).

---

## 4. Гейты (на каждом переносе)

- Firewall: `grep -RInE 'AFC|Aros Financial Core|LacMusa|Fiat Anchor|Crypto Anchor'` → пусто.
- Лексика: `grep -RIniE 'reward|incentive|stimulus'` → пусто.
- Валидность JSON/сборка где применимо.

---

## 5. Инварианты (стержень канона)

- **I1** — единица ARO возникает только как следствие PoT-вердикта `verified===1`.
- **I2** — born-and-burned: процессная часть чеканится и атомарно сжигается; нетто = 0.
- **I3** — оплата за подтверждённую работу, post-factum; заработанное удерживается. Без reward/incentive.
- **I4** — резерв собственный AST; индекс только от подтверждённого объёма.
- **I5** — детерминизм: всё воспроизводимо из NodeChain.
- **I6** — нет спекулятивной поверхности (ни потолка, ни стейкинга, ни голосования балансом, ни волатильности).
- **I7** — All-Seeing-Eye наблюдает и **имеет вето**, но никогда не инициирует.
- **I8** — причина пишется в NodeChain до признания эффекта.

---

**Жду твоего «го» по Фазе 1.** После утверждения — начинаю Фазу 2 (перенос 10 слоёв) отдельным PR.
