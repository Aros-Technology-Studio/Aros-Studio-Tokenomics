# Block H — Explicitly **not** the AST core track

**Purpose:** Prevent mixing outer-product, market, BFT-mainnet, or Eye-executive work into AST completion (A–G · E · F · I).  
**Rule:** Do not open freelanced PRs for H1–H4 as “AST core Done.”  
**Law:** Core Canon wins; Eye observe-only; portal edge never mints.

---

## Table

| ID | Item | Note | Priority |
|----|------|------|----------|
| **H1** | Outer API Contract (execution process shell) | Separate product **on top of** AST — not Core SoT, not portal freelancing | **later / out of AST core** |
| **H2** | Public market listing of ARO | Competing market player posture — forbidden for this product framing | **out** |
| **H3** | Multi-node BFT mainnet | Consensus product beyond single-writer journal + catch-up | **later** |
| **H4** | Eye veto / executive powers | Canon: All-Seeing Eye observes and notifies only | **out** (forbidden) |

---

## What H is not

| Confusion | Clarification |
|-----------|----------------|
| H1 vs portal OpenAPI | Portal edge APIs (`portal/openapi`) are **AST edge**, not “outer execution shell” |
| H3 vs F3 | F3 residual is **replication mesh / catch-up ops**; H3 is **BFT multi-writer mainnet** |
| H4 vs Eye HTTP | `GET /v1/core/eye/*` is observe/stream only — no veto endpoints |

---

## If work is needed later

1. New **product** charter (H1) with its own repo or `docs/` tree — not fake “layer Done” on 01–10.  
2. Canon **formal amendment** before any Eye executive capability (H4 would require reversing hard law — do not treat as backlog ticket).  
3. H2 stays **out** unless product mission is redefined by owner + Canon rewrite.  
4. H3 only after explicit multi-writer design under Canon (journal immutability, no silent fork merge).

---

## Acceptance for Block H (docs)

| Check | Evidence |
|-------|----------|
| Listed on completion track as non-AST-core | `COMPLETION-TRACK.md` §H |
| BACKLOG / ROADMAP do not schedule H2/H4 as AST core | those files |
| This file exists | path above |

**No code deliverable required for H.**
