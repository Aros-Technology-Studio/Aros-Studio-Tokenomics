# MODEL — `velocity-tracker`

Inputs: confirmed process volume (24h window), circulating supply from token ledger.  
Output: velocity scalar (UTC windows). Deterministic from NodeChain-derived inputs.
