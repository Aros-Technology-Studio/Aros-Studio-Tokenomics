# CONTRACT — `release-daemon`

**In:** reserveIndex, velocity, config keys `release.threshold`, `release.target`  
**Out:** phase initiation event / call into `release`  
**Must not:** flip phase without NodeChain + governance rules in `release` pack  
