# Non-goals

- No successful `veto()` path — always throws  
- No substitute for module fail-closed (PoT, L1, kill-switch remain first-line)  
- No Eye-owned free mint / burn / commission rewrite  
- No product “AI hierarchy” product UI required for v1 (bus + history suffice)  
- No Kafka requirement for v1 (file/memory durable stream is enough; multi-region later)
