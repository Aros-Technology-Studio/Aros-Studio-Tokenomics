# API

```
encodeProcessTx(input) → EncodedProcessTx
decodeProcessTx(encoded, expectedHash?) → DecodeResult
verifyEncodedPackage(pkg) → boolean
packageSigningDigest(pkg) → hex
EncodingService.encode / decode / verify / signPackage / verifyPackageSignature / listSchemas
```
