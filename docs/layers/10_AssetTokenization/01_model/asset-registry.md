# Asset registry

Working map `assetId → { processIdPrimary, currentValue, holderIds, status }`.

Registration journals `param_change` kind `asset_registered` with optional `documentPackageHash`.

Not SoT alone — reconstructible from process + token facts.
