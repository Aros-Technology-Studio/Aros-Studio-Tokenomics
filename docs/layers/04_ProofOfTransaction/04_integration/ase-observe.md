# Integration — ASE

PoT does not call Eye veto.  
Callers (pipeline) observe:

- `POT_VERIFIED` / `POT_REJECTED`  
- reason codes on failure  
- double-confirm attempts  

ASE has no authority to rewrite verdicts.
