export default function HistoryPage() {
  return (
    <main style={{ padding: '2rem' }}>
      <h1>History & audit</h1>
      <p>
        NodeChain-derived events for the current institution (not full network
        history).
      </p>
    </main>
  );
}
